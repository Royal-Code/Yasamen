# App Specs

Esta pasta guarda specs concretas de apps consumidores que usam Yasamen.

Regras:

- novas `app specs` devem nascer em `specs/apps/<slug>/`
- `app spec` define convenções do app e relação com telas
- detalhamento de tela vai para `screen spec`
