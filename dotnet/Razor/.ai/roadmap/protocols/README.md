# Protocolos de UI

Esta pasta guarda material de referência dos protocolos agnósticos usados como base para shell, `Page Pattern` e composição por zonas e `UIP-*`.

Escopo atual:

- protocolos operacionais de shell;
- protocolos operacionais de página;
- protocolos operacionais de composição visual;
- templates históricos de apoio.

Regra:

- estes arquivos servem como referência de origem;
- eles não fazem parte da camada final de `ui-map`;
- a consolidação ativa para o fluxo atual acontece em [page-patterns.md](/c:/git/github/royal-code/Yasamen/dotnet/Razor/.ai/ui-map/page-patterns.md), [shell-patterns.md](/c:/git/github/royal-code/Yasamen/dotnet/Razor/.ai/ui-map/shell-patterns.md) e [ui-map.md](/c:/git/github/royal-code/Yasamen/dotnet/Razor/.ai/ui-map/ui-map.md).

