# Instructions Transversais

Este grupo reúne instruções genéricas, guardrails e regras operacionais que não pertencem a um único domínio.

Escopo típico:

- regras de roteamento;
- regras de execução e validação;
- convenções reaproveitadas por múltiplos fluxos.

