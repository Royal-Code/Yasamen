# Redirect - Legacy `spec.md`

Este arquivo foi mantido por compatibilidade.

Use:

- `.ai/lib-spec.md` para componentes, pacotes e specs de biblioteca;
- `.ai/screen-spec.md` para telas e páginas;
- `.ai/station.md` como entrada única acima dos dois.

Exemplo recomendado:

```text
Leia `.ai/station.md` e crie o componente `button-group`.
```
