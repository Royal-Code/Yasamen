# Blueprints table

## Triagem

{Populada na seção 2 (Triagem) do protocolo — classificação inicial por pattern. Separar por grupos de patterns.}

### {Pattern Group Name}

| Pattern Id | Pattern Name | Classificação inicial | Justificativa |
|---|---|---|---|

{Classificação: `não gerar` | `resumido` | `completo`.}
{Justificativa: 1 frase curta amarrada à evidência do `{pattern}.ui-map.md` (nota geral, esforço, tipo de gap).}

## Resultado da geração

{Populada na seção 3 (Geração) — atualizada por iteração. Não inclui patterns triados inicialmente como `não gerar`; inclui patterns reclassificados para `não gerar` durante a síntese. Separar por grupos de patterns.}

### {Pattern Group Name}

| Pattern Id | Classificação final | Status | Tipo de artefato | Componentes propostos | Limites declarados |
|---|---|---|---|---|---|

{Tipo de artefato: `composição apenas` | `wrapper/componente leve` | `componente novo único` | `compound de componente novo` | `conjunto de componentes novos atômicos coordenados` | `componente novo standalone`.}
{Status: `gerado` | `reclassificado` | `bloqueado`.}
{Componentes propostos: lista separada por vírgula (ex: `Section, EmptyState, ErrorState`), ou `-` quando `composição apenas`.}
{Limites declarados: resumo curto dos `Limites` declarados no blueprint (1-2 itens mais relevantes).}
